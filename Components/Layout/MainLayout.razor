@inherits LayoutComponentBase

<RadzenComponents @rendermode="InteractiveServer" />

<RadzenLayout>
    @* <RadzenHeader>
        <RadzenStack Orientation="Radzen.Orientation.Horizontal" AlignItems="Radzen.AlignItems.Center" Gap="0" JustifyContent="JustifyContent.SpaceBetween">
            <RadzenSidebarToggle Click="@(() => sidebarExpanded = !sidebarExpanded)" />
            @* <RadzenLabel Text="Header" /> 

            <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" Gap="3" JustifyContent="JustifyContent.Right">
                <RadzenButton Click="@(args => OnClick())" Text="Primary" ButtonStyle="ButtonStyle.Primary" />
                <RadzenButton Click="@(args => OnClick())" Text="Primary" ButtonStyle="ButtonStyle.Primary" />
                <RadzenButton Click="@(args => OnClick())" Text="Primary" ButtonStyle="ButtonStyle.Primary" />
            </RadzenStack>
        </RadzenStack>
    </RadzenHeader> *@
    @* <RadzenSidebar Responsive="false" Style="width: max-content">
        <RadzenPanelMenu ShowArrow="false">

            <RadzenPanelMenuItem Text="Administración" Icon="inventory" Path="/admin" />
            <RadzenPanelMenuItem Text="Registrarse" Icon="security" Path="Account/Register" />
            <RadzenPanelMenuItem Text="Iniciar sesión" Icon="computer" Path="Account/Login" />
            
        </RadzenPanelMenu>
    </RadzenSidebar> *@
    <RadzenBody>
        <div class="rz-p-4">
            @Body
        </div>
    </RadzenBody>
</RadzenLayout>
